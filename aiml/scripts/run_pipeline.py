"""Thin batch pipeline CLI entry point."""

from sif_engine.pipeline import run_batch


if __name__ == "__main__":
    raise SystemExit("Load reports and call run_batch(reports) once Stage 1-4 implementations are available.")