# Base URL of the FastAPI backend (include the /api/v1 prefix).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1

# When "true", the app serves local fixture data instead of calling the backend.
# Use this for offline demos / when Postgres + uvicorn are not running.
NEXT_PUBLIC_USE_MOCKS=false
